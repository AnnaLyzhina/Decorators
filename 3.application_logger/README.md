# Application logger

В этом задании декоратор `logger(path)` применён к функциям приложения из прошлой домашней работы.

Логируются:
- вызовы функций бота;
- вызовы функций работы с данными;
- аргументы функций и возвращаемые значения.

Лог записывается в файл `tg_api.log`.